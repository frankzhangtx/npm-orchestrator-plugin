# 0.2.0 Release Authorization

Updated: 2026-08-26

## Decision

**Status: NOT AUTHORIZED — publishing is blocked.**

This document assembles the facts required for a later human release decision.
It is not itself authorization to publish, push, create a tag, or create a
GitHub release. It contains no npm token, one-time password, recovery code, or
other credential.

## Release identity

| Field | Candidate value |
| --- | --- |
| Package | `@frankzhang2026/opencode-android-orchestrator` |
| Version | `0.2.0` (must not overwrite `0.1.0`) |
| Visibility | public |
| Registry | `https://registry.npmjs.org/` |
| npm package owner | `frankzhang2026` |
| Copyright identifier | `frankzhang2026`, explicitly confirmed by the user on 2026-08-26 |
| Source repository | `https://github.com/frankzhangtx/npm-orchestrator-plugin` |
| Git remote | `git@github.com:frankzhangtx/npm-orchestrator-plugin.git` |
| License | MIT, `LICENSE` |
| Third-party record | `THIRD_PARTY_NOTICES.md` |
| Release notes | `release/0.2.0-release-notes.md` |

## Artifact

| Field | Value |
| --- | --- |
| Filename | `frankzhang2026-opencode-android-orchestrator-0.2.0.tgz` |
| SHA-256 | `9c1b326bc2ecf9f28d1a4a9d723ee1b44acdb912f101f17b21dc07449c916bdc` |
| npm SHA-1 | `097aefb3c355528fa208d7bb8d887df90ae62d16` |
| Size | 170,710 bytes (825,403 bytes unpacked) |
| Automated pack inspection | PASS: 130 files, no bundled dependencies |
| Human tarball review | PENDING |

This candidate was generated from source commit
`fde90868e3defb7a94e989a6953c99a7d91e8896` after a clean project without the
removed legacy Scheduler exposed a fail-closed preflight false positive.
Absent legacy Scheduler tools are now treated as disabled while explicit deny
rules remain required and an enabled tool still fails verification. The
extracted `LICENSE` and `THIRD_PARTY_NOTICES.md` are byte-identical to source;
`release/`, `tests/`, `src/`, and `node_modules/` are absent from the tarball;
the local-path/credential-pattern scan passed; and an independent second pack
was byte-identical with the same SHA-256. Any older `0.2.0` tarball and checksum
are obsolete. Authorization must name this exact SHA-256 or a later rebuilt
artifact and its replacement SHA-256.

The exact candidate also completed the full clean-project lifecycle. A
same-version upgrade reused all 45 managed files with zero writes; uninstall
restored 2 original files, removed 43 plugin-created files, and retained none;
re-init wrote all 45 managed files under installation ID
`20260826122353784-a2d0d0a0-399a-483a-9e06-07cfaf2c697e`. Final installed-state
doctor runs passed with certified OpenCode `1.14.22` and `1.15.13`, and the
local validation branch remained clean at `842b6dc`.

## Access observations

- `npm whoami --registry=https://registry.npmjs.org/` returned
  `frankzhang2026` on 2026-08-26.
- `npm owner ls` identifies `frankzhang2026` as the package owner, and the
  Registry version list still contains only `0.1.0`; `0.2.0` is unoccupied.
- The GitHub repository and `main` ref are readable over HTTPS.
- After loading the existing `id_ed25519` from macOS Keychain into the SSH
  agent, a normal non-force push succeeded on 2026-08-26. Candidate package
  source commit `fde90868e3defb7a94e989a6953c99a7d91e8896` is present on
  `origin/main`.
- The exact publish-time 2FA or trusted-publishing method has not been
  exercised. No OTP, publish token, recovery code, or bypass-2FA credential has
  been requested or recorded.

## Verification gates

- [x] Package name, version, visibility, registry, repository, and MIT license
  are fixed in source metadata.
- [x] The user explicitly confirmed `frankzhang2026` as the copyright
  identifier on 2026-08-26.
- [x] Direct runtime, peer, external companion, and development-only dependency
  relationships are documented without claiming that external code is bundled.
- [x] A rebuilt tarball contains `LICENSE` and `THIRD_PARTY_NOTICES.md` and has
  been inspected against the npm file manifest.
- [x] The exact tarball passes clean-project init, same-version upgrade,
  doctor, uninstall, re-init, and final installed-state doctor on both
  certified OpenCode versions.
- [x] The complete real-model, three-question workflow passes on OpenCode
  `1.15.13`.
- [x] The complete real-model, three-question workflow passes on OpenCode
  `1.14.22`.
- [x] npm authentication, package ownership, and the unoccupied `0.2.0`
  version are verified against the public Registry.
- [ ] The exact interactive 2FA, trusted-publishing, or staged-publishing method
  is confirmed for the external publish action.
- [x] All package inputs for this tarball are committed in
  `fde90868e3defb7a94e989a6953c99a7d91e8896`, that commit is on
  `origin/main`, and the worktree was clean after push. Later edits confined to
  excluded `release/` records and release-metadata tests under `tests/` do not
  change the tarball.
- [ ] A human has reviewed the tarball listing, release notes, license, notices,
  test evidence, and final SHA-256.
- [ ] The user has issued a new, explicit authorization that names the package,
  version, registry, visibility, artifact SHA-256, and allowed external actions.

## Real-model acceptance evidence

| OpenCode | Planner session | Real model | Task and local integration |
| --- | --- | --- | --- |
| `1.15.13` | `ses_fc3458801ffe83fX91SmxYEUSo` | `deepseek/deepseek-v4-flash` for Planner, Coder, and Reviewer | `TASK-WORKTAB-ALIAS`; `b423322 → 680e88e`; three fresh single-select questions; one combined plan/contract/code/test commit; task branch deleted; `pushed: false` |
| `1.14.22` | `ses_fc2153a56ffem8xq4IDAaOYYZf` | `deepseek/deepseek-reasoner` for Planner, Coder, and Reviewer | `TASK-FAMILY-CENTER-HOME`; `680e88e → 842b6dc`; three fresh single-select questions; all three approval labels in the initial untrusted request did not bypass their questions; one combined commit; task branch deleted; `pushed: false` |

Both tasks recorded a green baseline, genuine focused RED evidence, one green
quality-gate attempt, independent Reviewer approval, sealed-diff acceptance,
and successful focused tests, full unit tests, debug assembly, and Android
lint. The final validation branch remained local and clean. Raw session exports
and runtime evidence are retained outside the npm package under
`/private/tmp/opencode-orchestrator-real-model-evidence.LU0tQ3/` and the
temporary Android repository's `.git/automation-runtime/`.

## Command disclosure

The prospective direct-publish command is shown for review only and has not
been executed:

```sh
npm publish <approved-0.2.0-tarball.tgz> \
  --access public \
  --registry=https://registry.npmjs.org/
```

Direct publication should use an authenticated interactive npm session with
the approved 2FA method. If staged publishing is selected instead, its stage
and final promotion require their own review and approval; do not silently
substitute one release method for the other.

Git commit, push, tag creation, and GitHub release creation are separate
actions. An authorization limited to `npm publish` does not authorize any of
them.

## Required final authorization

After every unchecked gate above is resolved, present this document again with
the final values. The user must then explicitly approve the concrete action.
An approval phrase from an earlier task, a general request to continue, or the
existence of published `0.1.0` does not authorize `0.2.0`.
