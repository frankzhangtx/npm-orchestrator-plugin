# 0.2.0 Release Authorization

Updated: 2026-08-26

## Decision

**Status: NOT AUTHORIZED — publishing is blocked.**

This document assembles the facts required for a later human release decision.
It is not itself authorization to publish, push, create a tag, or create a
GitHub release. It contains no npm token, one-time password, recovery code, or
other credential.

## Release identity

| Field | Candidate value |
| --- | --- |
| Package | `@frankzhang2026/opencode-android-orchestrator` |
| Version | `0.2.0` (must not overwrite `0.1.0`) |
| Visibility | public |
| Registry | `https://registry.npmjs.org/` |
| npm package owner | `frankzhang2026` |
| Copyright identifier | `frankzhang2026`, explicitly confirmed by the user on 2026-08-26 |
| Source repository | `https://github.com/frankzhangtx/npm-orchestrator-plugin` |
| Git remote | `git@github.com:frankzhangtx/npm-orchestrator-plugin.git` |
| License | MIT, `LICENSE` |
| Third-party record | `THIRD_PARTY_NOTICES.md` |
| Release notes | `release/0.2.0-release-notes.md` |

## Artifact

| Field | Value |
| --- | --- |
| Filename | `frankzhang2026-opencode-android-orchestrator-0.2.0.tgz` |
| SHA-256 | `3c80aa6b72ccd14bfd15d0ecf4de13e92f8661c356d1659b3737e2b37b8a8d94` |
| Size | 170,658 bytes (825,169 bytes unpacked) |
| Automated pack inspection | PASS: 130 files, no bundled dependencies |
| Human tarball review | PENDING |

This candidate was generated after adding the license and notice files. The
extracted `LICENSE` and `THIRD_PARTY_NOTICES.md` are byte-identical to source;
`release/`, `tests/`, `src/`, and `node_modules/` are absent from the tarball;
the local-path/credential-pattern scan passed; and an independent second pack
was byte-identical with the same SHA-256. Any older `0.2.0` tarball and checksum
are obsolete. Authorization must name this exact SHA-256 or a later rebuilt
artifact and its replacement SHA-256.

## Access observations

- `npm owner ls` identifies `frankzhang2026` as the existing package owner.
- `npm whoami` returned `E401 Unauthorized` on 2026-08-26. The current shell is
  not authenticated for publishing.
- The GitHub repository and `main` ref are readable over HTTPS.
- The configured SSH remote returned `Permission denied (publickey)` in the
  current shell. Push permission has not been demonstrated.
- Account-level and package-level 2FA state has not been verified. No publish
  credential or bypass-2FA token has been requested or recorded.

## Verification gates

- [x] Package name, version, visibility, registry, repository, and MIT license
  are fixed in source metadata.
- [x] The user explicitly confirmed `frankzhang2026` as the copyright
  identifier on 2026-08-26.
- [x] Direct runtime, peer, external companion, and development-only dependency
  relationships are documented without claiming that external code is bundled.
- [x] A rebuilt tarball contains `LICENSE` and `THIRD_PARTY_NOTICES.md` and has
  been inspected against the npm file manifest.
- [ ] The complete real-model, three-question workflow passes on OpenCode
  `1.15.13`.
- [ ] The complete real-model, three-question workflow passes on OpenCode
  `1.14.22`.
- [ ] npm authentication, package write permission, and the exact interactive
  2FA or staged-publishing method are verified.
- [ ] The candidate source is committed, the worktree is clean, and the commit
  intended for release is identified.
- [ ] A human has reviewed the tarball listing, release notes, license, notices,
  test evidence, and final SHA-256.
- [ ] The user has issued a new, explicit authorization that names the package,
  version, registry, visibility, artifact SHA-256, and allowed external actions.

## Command disclosure

The prospective direct-publish command is shown for review only and has not
been executed:

```sh
npm publish <approved-0.2.0-tarball.tgz> \
  --access public \
  --registry=https://registry.npmjs.org/
```

Direct publication should use an authenticated interactive npm session with
the approved 2FA method. If staged publishing is selected instead, its stage
and final promotion require their own review and approval; do not silently
substitute one release method for the other.

Git commit, push, tag creation, and GitHub release creation are separate
actions. An authorization limited to `npm publish` does not authorize any of
them.

## Required final authorization

After every unchecked gate above is resolved, present this document again with
the final values. The user must then explicitly approve the concrete action.
An approval phrase from an earlier task, a general request to continue, or the
existence of published `0.1.0` does not authorize `0.2.0`.
