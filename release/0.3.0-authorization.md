# 0.3.0 Release Authorization and Outcome

Updated: 2026-08-28

## Decision

**Status: PUBLISHED — verified on the npm Registry.**

The user explicitly instructed publication of the new npm version `0.3.0` on
2026-08-28. After the first browser authorization page became unresponsive, the
waiting publish command was cancelled, the Registry was checked to confirm that
`0.3.0` had not been written, and the user later explicitly instructed the task
to continue. The successful attempt used npm's browser authorization flow in
Safari. This authorization covered the public npm publication only; it did not
authorize a Git tag or GitHub release. No npm token, one-time password, recovery
code, browser-auth URL, or other credential is recorded here.

## Release identity

| Field | Released value |
| --- | --- |
| Package | `@frankzhang2026/opencode-android-orchestrator` |
| Version | `0.3.0` |
| Visibility | public |
| Registry | `https://registry.npmjs.org/` |
| npm package owner | `frankzhang2026` |
| Source repository | `https://github.com/frankzhangtx/npm-orchestrator-plugin` |
| Candidate source commit | `4a54255edd486266fd1b7f98a68b7ee03375e203` |
| Git remote | `git@github.com:frankzhangtx/npm-orchestrator-plugin.git` |
| License | MIT, `LICENSE` |
| Third-party record | `THIRD_PARTY_NOTICES.md` |
| Release notes | `release/0.3.0-release-notes.md` |

## Artifact

| Field | Value |
| --- | --- |
| Filename | `frankzhang2026-opencode-android-orchestrator-0.3.0.tgz` |
| SHA-256 | `d09d7bd38915cc555fca5d77ca0accb571616788edeee87f42e1cee7da742cfe` |
| npm SHA-1 | `84b55b436a9ae99d3dde69b9d82d25b59b42d541` |
| npm integrity | `sha512-whkfJQN7ynvWYg5olieGm+lHoj/wkiKl13bRQqbohVFO/OE8rvaIljDxn2u3xPRc7ZiJ19tIYC+hrOJ7zeHkVw==` |
| Size | 177,010 bytes compressed; 859,854 bytes unpacked |
| Automated pack inspection | PASS: 130 files |

The release candidate was packed twice independently from the same clean source
commit and both files were byte-identical. `release/`, `tests/`, `src/`,
`node_modules/`, `.env`, and `.npmrc` were absent; no absolute user path,
credential pattern, private key, or npm token was found. The packaged `LICENSE`
and `THIRD_PARTY_NOTICES.md` were byte-identical to their source files. After
publication, a fixed-version Registry download was byte-identical to the exact
candidate and had the same SHA-256 and 177,010-byte size.

## Access observations

- `npm whoami --registry=https://registry.npmjs.org/` returned
  `frankzhang2026` immediately before publication, and npm ownership had been
  verified for that account.
- Immediately before the successful attempt, the Registry contained `0.1.0`
  and `0.2.0`, so `0.3.0` was unoccupied. After publication it contains
  `0.1.0`, `0.2.0`, and `0.3.0`, with `latest` pointing to `0.3.0`.
- Registry metadata records `0.3.0` at `2026-08-28T07:51:57.838Z`.
- The first browser-auth publish attempt was cancelled after the browser became
  unresponsive. A Registry read confirmed that it produced no publication.
  The successful retry used Safari and returned normally.
- All package inputs were committed in
  `4a54255edd486266fd1b7f98a68b7ee03375e203`, that commit was on
  `origin/main`, and the worktree was clean before publication. Release records
  and their tests are excluded from the npm package and do not change the
  published artifact.

## Verification gates

- [x] Package name, `0.3.0` version, public visibility, Registry, repository,
  and MIT license were fixed in source metadata.
- [x] TypeScript type checking passed.
- [x] All 124 Node tests passed.
- [x] All 38 audited Shell lifecycle cases passed.
- [x] `npm run pack:check` passed with a 130-file package manifest.
- [x] Two independent local packs were byte-identical.
- [x] The exact local tarball passed clean-consumer import, API, and CLI checks.
- [x] npm authentication, ownership, and the unoccupied version were verified
  against the public Registry immediately before publication.
- [x] The candidate source commit was pushed to `origin/main` before the
  external publication.
- [x] A fixed Registry download is byte-identical to the exact candidate.
- [x] A clean consumer installed the fixed Registry version, reported zero
  audit vulnerabilities, loaded the plugin and API, reported version `0.3.0`,
  and reported default module scope `all`.

## Publication outcome

- `npm publish` returned
  `+ @frankzhang2026/opencode-android-orchestrator@0.3.0`.
- Registry metadata reports npm SHA-1
  `84b55b436a9ae99d3dde69b9d82d25b59b42d541`, integrity
  `sha512-whkfJQN7ynvWYg5olieGm+lHoj/wkiKl13bRQqbohVFO/OE8rvaIljDxn2u3xPRc7ZiJ19tIYC+hrOJ7zeHkVw==`,
  130 files, and `latest` set to `0.3.0`.
- A fixed-version Registry download is byte-identical to the published
  candidate and has SHA-256
  `d09d7bd38915cc555fca5d77ca0accb571616788edeee87f42e1cee7da742cfe`.
- No Git tag or GitHub release was created.

## Executed action

The successful direct-publish action targeted the exact artifact recorded
above:

```sh
npm publish <sha256-d09d7bd3...42cfe-0.3.0-tarball.tgz> \
  --access public \
  --registry=https://registry.npmjs.org/
```

This authorization is consumed by the recorded `0.3.0` publication and does
not carry forward to another version or any unrecorded external action.
