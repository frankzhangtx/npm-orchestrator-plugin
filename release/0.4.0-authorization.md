# 0.4.0 Release Authorization and Outcome

Updated: 2026-08-29

## Decision

**Status: PUBLISHED — verified on the npm Registry.**

The user explicitly instructed publication of the new npm version `0.4.0` on
2026-08-29. npm's official browser login and publish authorization flows were
completed, and the exact reviewed tarball was uploaded once. This authorization
covered the public npm publication only; it did not authorize a Git tag or
GitHub release. No npm token, one-time password, recovery code, browser-auth
URL, or other credential is recorded here.

## Release identity

| Field | Released value |
| --- | --- |
| Package | `@frankzhang2026/opencode-android-orchestrator` |
| Version | `0.4.0` |
| Visibility | public |
| Registry | `https://registry.npmjs.org/` |
| npm package owner | `frankzhang2026` |
| Source repository | `https://github.com/frankzhangtx/npm-orchestrator-plugin` |
| Candidate source commit | `ae22486d62cdfb5a8aa6a9fcafe41087546124a7` |
| Git remote | `git@github.com:frankzhangtx/npm-orchestrator-plugin.git` |
| License | MIT, `LICENSE` |
| Third-party record | `THIRD_PARTY_NOTICES.md` |
| Release notes | `release/0.4.0-release-notes.md` |

## Artifact

| Field | Value |
| --- | --- |
| Filename | `frankzhang2026-opencode-android-orchestrator-0.4.0.tgz` |
| SHA-256 | `dcb50806f173ceab97d1ac393363724ded56f37e4e54f754cdf04a96bb10a299` |
| npm SHA-1 | `1ee666a298126e52d3e022a11723f57c0781e73a` |
| npm integrity | `sha512-R4fHYMkVdMxKmaNQk4KNqRDYEbyK/GOpzDHrbM21w1leqjEJ+VmfLs26klmXd95VnY7TiFP/NDLtz0x/z7qp5w==` |
| Size | 182,446 bytes compressed; 885,239 bytes unpacked |
| Automated pack inspection | PASS: 130 files |

The release candidate was packed twice independently from the same clean source
commit and both files were byte-identical. `release/`, `tests/`, `src/`,
`node_modules/`, `.env`, and `.npmrc` were absent; no absolute user path,
private-key marker, npm authentication token, or credential assignment was
found. The packaged `LICENSE` and `THIRD_PARTY_NOTICES.md` were byte-identical
to their source files. After publication, a fixed-version Registry download was
byte-identical to the exact candidate and had the same SHA-256 and 182,446-byte
size.

## Access observations

- `npm whoami --registry=https://registry.npmjs.org/` returned
  `frankzhang2026` immediately before publication, and `npm owner ls` confirmed
  that account owns the package.
- Immediately before publication, the Registry contained `0.1.0`, `0.2.0`, and
  `0.3.0`, so `0.4.0` was unoccupied. After publication it contains `0.1.0`,
  `0.2.0`, `0.3.0`, and `0.4.0`, with `latest` pointing to `0.4.0`.
- Registry metadata records `0.4.0` at `2026-08-29T09:33:59.875Z`.
- All package inputs were committed in
  `ae22486d62cdfb5a8aa6a9fcafe41087546124a7`, that commit was on
  `origin/main`, and the worktree was clean before publication. Release records
  and their tests are excluded from the npm package and do not change the
  published artifact.

## Verification gates

- [x] Package name, `0.4.0` version, public visibility, Registry, repository,
  and MIT license were fixed in source metadata.
- [x] TypeScript type checking passed.
- [x] All 125 Node tests passed.
- [x] All 42 audited Shell lifecycle cases passed.
- [x] `npm run pack:check` passed with a 130-file package manifest.
- [x] Two independent local packs were byte-identical.
- [x] The exact local tarball passed clean-consumer root-plugin, API-version,
  CLI-help, and zero-vulnerability audit checks.
- [x] npm authentication, ownership, and the unoccupied version were verified
  against the public Registry immediately before publication.
- [x] The candidate source commit was pushed to `origin/main` before the
  external publication.
- [x] A fixed Registry download is byte-identical to the exact candidate.
- [x] A clean consumer installed the fixed Registry version, reported zero
  audit vulnerabilities, loaded the plugin and API, reported version `0.4.0`,
  and loaded the CLI help successfully.

## Publication outcome

- `npm publish` returned
  `+ @frankzhang2026/opencode-android-orchestrator@0.4.0`.
- Registry metadata reports npm SHA-1
  `1ee666a298126e52d3e022a11723f57c0781e73a`, integrity
  `sha512-R4fHYMkVdMxKmaNQk4KNqRDYEbyK/GOpzDHrbM21w1leqjEJ+VmfLs26klmXd95VnY7TiFP/NDLtz0x/z7qp5w==`,
  130 files, and `latest` set to `0.4.0`.
- A fixed-version Registry download is byte-identical to the published
  candidate and has SHA-256
  `dcb50806f173ceab97d1ac393363724ded56f37e4e54f754cdf04a96bb10a299`.
- No Git tag or GitHub release was created.

## Executed action

The successful direct-publish action targeted the exact artifact recorded
above:

```sh
npm publish <sha256-dcb50806...0a299-0.4.0-tarball.tgz> \
  --access public \
  --registry=https://registry.npmjs.org/
```

This authorization is consumed by the recorded `0.4.0` publication and does
not carry forward to another version or any unrecorded external action.
